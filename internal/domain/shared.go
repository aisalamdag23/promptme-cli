package domain

const (
	EXIT = "exit"

	// LLM_RESPONSE_CONTEXT used to initialize chat session context
	LLM_RESPONSE_CONTEXT = "only return response that are related to %s and the likes"

	LLM_PROVIDER_GEMINI = "gemini"
)
